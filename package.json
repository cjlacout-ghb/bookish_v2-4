{
  "name": "bookish-root",
  "version": "2.4.0",
  "scripts": {
    "install:all": "npm install --prefix frontend && pip install fastapi uvicorn sqlalchemy python-multipart",
    "dev": "concurrently \"npm run dev --prefix frontend\" \"cd backend && py -m uvicorn main:app --reload --port 8000\"",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "cd backend && py -m uvicorn main:app --reload --port 8000"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
